- Validation logic is broken
- Retry button doesn't work
- Success screen has logo offcentre
- There seems to be a mystery `unity/Assets/CPUWrapper/level1.txt` file that's being used instead of the `unity/Assets/Levels` files. 
- How do the level json files have R1, R2, etc. instead of x1, x2, etc.?
- Edge case: press T while carrying brick. 
- Edge case: person takes brick off spawn, but we have a stall expected when they press T to validate. Then, they can't put their brick back on spawn since another brick has replaced

Need to update skill so that it doesn't edit files with tool calls
Need to update skill so that it always gives me file names and line numbers to look at.